import { light as lightColor, dark as darkColor } from './colors'
import borderRadius from './border-radius'

export default {
  dark: {
    borderRadius,
    colors: darkColor,
  },
  light: {
    borderRadius,
    colors: lightColor,
  },
}
